{
  "name": "shop-api-starter",
  "version": "1.0.0",
  "description": "AI-Enhanced QA Workshop — ShopAPI Capstone",
  "type": "module",
  "scripts": {
    "dev": "node src/server.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "mutate": "stryker run",
    "lint": "eslint src/ tests/",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.0.0",
    "@stryker-mutator/vitest-runner": "^9.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "fast-check": "^3.22.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "playwright": "^1.50.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "lint-staged": {
    "*.mjs": ["eslint --fix"]
  }
}
